# Optional: change where project data is stored.
# UI_AI_CREATOR_DB_PATH=./data/projects.db

# Optional: run the Hono server on a different port.
# PORT=3000

# Codex uses the server process user's existing login session.
# Run `codex login` as that user before using generation features.
